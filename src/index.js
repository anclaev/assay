import "./index.sass";

import React from "react";
import ReactDOM from "react-dom";
import Layout from "./hoc/Layout";

ReactDOM.render(
  <React.StrictMode>
    <Layout />
  </React.StrictMode>,
  document.body
);
